/**
 * Enum constants, same as "screens" in Tailwindcss for JS/React use
 * @enum number
 */
enum Breakpoints {
    MOBILE = 480,
    SM = 640,    
    MD = 768,    
    LG = 1024,    
    XL = 1280,   
    XXL = 1536   
}

export default Breakpoints;